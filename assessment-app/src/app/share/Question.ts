export default class Question {
    assessmentId: Number;
    question: [ {
        questionTitle: String;
        description?: String;
        questionID: Number;
        //options: [String];
    }];
}